# ⚠️ Historical — pre-convergence planning docs

The plans under this directory are the **original (2026-06-05) Enstellar build
plans**, written against the now-archived standalone `/Users/.../Enstellar/`
layout (absolute paths, the `@enstellar/canonical-model` package name, etc.).

They are kept for history only and are **superseded** by:
- the live monorepo services (`services/enstellar-*`), and
- the current planning under `docs/superpowers/`.

See the repo-root `CONVERGENCE.md`. Do not treat these as current.
