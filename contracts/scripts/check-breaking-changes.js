#!/usr/bin/env node
// Placeholder: will use openapi-diff or similar when packages are installed.
// For now, exit 0 so CI passes until the implementation is complete.
console.log("Contract breaking-change check: placeholder (no packages installed yet)");
process.exit(0);
