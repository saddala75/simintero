[databases]
simintero = host=postgres port=5432 dbname=simintero
workflow  = host=postgres port=5432 dbname=workflow
keycloak  = host=postgres port=5432 dbname=keycloak
hapi      = host=postgres port=5432 dbname=hapi

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
# Development: MD5 for local convenience with plaintext passwords in userlist.dev.txt
# For production, use scram-sha-256 with SCRAM-SHA-256 hashes in userlist.txt
auth_type   = md5
auth_file   = /etc/pgbouncer/userlist.txt
pool_mode   = transaction
max_client_conn = 500
default_pool_size = 20
server_reset_query = DISCARD ALL
ignore_startup_parameters = extra_float_digits,search_path
