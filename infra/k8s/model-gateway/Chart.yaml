apiVersion: v2
name: model-gateway
description: Helm chart for the Simintero model-gateway
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: base-service
    version: "0.1.0"
    repository: "file://../charts/base-service"
    alias: base
